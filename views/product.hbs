<div class="product">
    <h1>{{product.title}}</h1>
    <img src="{{product.img}}" alt="{{product.title}}">
    <p class="price big">{{product.price}}</p>

    <p class="description flow-text">{{product.description}}</p>
 
</div>